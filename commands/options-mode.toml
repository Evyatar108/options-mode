description = "Control options mode (on/off/strict/auto/status)"
prompt = "Run /options-mode {{args}}. Valid subcommands: on, off, strict, auto, status, default. The hook handles this command and blocks it from reaching the model."
